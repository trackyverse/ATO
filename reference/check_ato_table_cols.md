# Check that the table columns match the prototype

Check that the table columns match the prototype

## Usage

``` r
check_ato_table_cols(object, ref)
```

## Arguments

- object:

  A prospective ATO slot object

- ref:

  The prototype for the respective slot

## Value

Nothing. Called to stop() if needed.
